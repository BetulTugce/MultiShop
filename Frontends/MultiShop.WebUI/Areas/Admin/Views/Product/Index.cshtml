@using MultiShop.WebUI.Models.ViewModels.Catalog.Product
@model List<ProductListVM>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
    int counter = 0;
}

<section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="chart-box">
                <h4>Ürün Listesi</h4>
                <p>Ürünler ile ilgili işlemlerinizi bu sayfadan gerçekleştirebilirsiniz.</p>
                <a href="/Admin/Product/CreateProduct" style="margin-bottom: 10px;" class="btn btn-primary">Yeni</a>
                <div class="bs-example" data-example-id="bordered-table">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Ürün Adı</th>
                                <th>Fiyatı</th>
                                <th>Güncelle</th>
                                <th>Sil</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var product in Model)
                            {
                                counter++;
                                <tr>
                                    <th scope="row">@counter</th>
                                    <td>@product.Name</td>
                                    <td>@product.Price</td>
                                    <td><a href="/Admin/Product/UpdateProduct/@product.Id" class="btn btn-warning">Güncelle</a></td>
                                    <td><a href="/Admin/Product/DeleteProduct/@product.Id" class="btn btn-danger">Sil</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>

